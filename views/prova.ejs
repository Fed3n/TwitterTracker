<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Twitter Analytics</title>
    </head>
<body>
    <div class="container">
        <div id="filterlabels">
            <div><button id="search">Search</button><input type="file" id="import" value="Import" /><button id="export">Export</button></div>
            <div>
                <label for="inputfilter">Tag:</label><input v-model="inputfilter"><button id="addlabel" v-on:click="add">Aggiungi</button>
            </div>
            <div id="labelscontainer">
                <div class="label" v-for="label in labels">
                    {{label}} <button class="delete" @click="remove(label)">x</button>
                </div>
            </div>
        </div>
        <div id="tweetslist">
            <table>
                <tr>
                    <th>Utente</th><th>Contenuto</th><th>Data-Ora</th><th></th>
                </tr>
                <tr v-for="tweet in tweets">
                    <td> {{tweet.user.name}} </td>
                    <td> {{tweet.text}} </td>
                    <td> {{tweet.createdAt}} </td>
                    <td> <button @click="showinfo(tweet)"> ... </button> </td>
                </tr>
            </table> 
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="/js/filterlabels.js"></script>
    <script src="/js/tweetslist.js"></script>
    <script src="/js/utility.js"></script>
</body>
</html>